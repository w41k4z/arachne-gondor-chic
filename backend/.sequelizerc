const path = require("path");

module.exports = {
  config: path.resolve("config", "config.js"),
  modelsPath: path.resolve("models" , "relation"),
  migrationsPath: path.resolve("database", "migrations"),
};
